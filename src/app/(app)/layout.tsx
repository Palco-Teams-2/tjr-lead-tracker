import { ApplicationShell2 } from "@/components/application-shell2";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <ApplicationShell2>{children}</ApplicationShell2>;
}
